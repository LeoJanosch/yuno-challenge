apiVersion: v1
kind: ServiceAccount
metadata:
  name: voyager-gateway
  namespace: voyager
  labels:
    app: voyager-gateway
  annotations:
    # EKS IRSA annotation - replace with actual role ARN
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/voyager-gateway-role
